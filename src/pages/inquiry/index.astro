---
import BaseLayout from "@/layouts/BaseLayout.astro";
const pageTitle = "研究の問合せ";
---

<BaseLayout pageTitle={pageTitle}>
  <div class="flex flex-col max-w-3xl mx-auto gap-5">
    <div>
      <p>
        近年、人工知能、計算知能、機械学習などに関する産業界からの問合せが多くなっており、うれしい限りです。
        残念ながら、これらの方法論は単に書籍などで勉強しただけではなかなか使えるようにはなっておりません。
        長年の研究経験から何かお役に立てることがあるかもしれませんので、問い合わせたり、依頼したりしたいことが何かございましたら、以下のフォームからご連絡ください。
      </p>
    </div>
    <!-- お問い合わせフォーム -->
    <section>
      <div>
        <form
          action="https://ssgform.com/s/pwXQTiah8wAn"
          method="post"
          class="space-y-4 border border-gray-200 rounded-lg p-6 bg-gray-50"
        >
          <!-- 名前とメールアドレスを横並びにするコンテナ -->
          <div class="flex flex-col sm:flex-row sm:space-x-4 space-y-6 sm:space-y-0">
            <!-- 名前 -->
            <div class="w-full">
              <label for="name" class="block text-sm font-medium text-gray-700 mb-2"> 名前 </label>
              <input
                type="text"
                id="name"
                name="name"
                placeholder="お名前"
                class="w-full px-4 py-2 border border-gray-300 rounded-md"
              />
            </div>

            <!-- メールアドレス -->
            <div class="w-full">
              <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
                メールアドレス
              </label>
              <input
                type="email"
                id="email"
                name="email"
                placeholder="メールアドレス"
                class="w-full px-4 py-2 border border-gray-300 rounded-md"
              />
            </div>
          </div>

          <!-- 件名 -->
          <div>
            <label for="subject" class="block text-sm font-medium text-gray-700 mb-2"> 件名 </label>
            <input
              type="text"
              id="subject"
              name="subject"
              placeholder="件名"
              class="w-full px-4 py-2 border border-gray-300 rounded-md"
            />
          </div>

          <!-- お問い合わせ内容 -->
          <div>
            <label for="message" class="block text-sm font-medium text-gray-700 mb-2">
              お問い合わせ内容
            </label>
            <textarea
              id="message"
              name="message"
              rows="5"
              placeholder="お問い合わせ内容"
              class="w-full px-4 py-3 border border-gray-300 rounded-md"></textarea>
          </div>

          <!-- reCAPTCHA -->
          <script is:inline src="https://www.google.com/recaptcha/api.js" async defer></script>
          <div class="g-recaptcha" data-sitekey="6LeQd48rAAAAAORn-UC6ifHik8OeV2lkOGAz5fI-"></div>

          <!-- 送信ボタン -->
          <div>
            <button
              type="submit"
              class="w-full py-3 px-4 bg-blue-600 hover:bg-blue-700 text-white rounded-md"
            >
              送信
            </button>
          </div>
        </form>
      </div>
    </section>
  </div>
</BaseLayout>
